---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bestpracticescanners.bps.openshift.io
spec:
  group: bps.openshift.io
  names:
    kind: BestPracticeScanner
    listKind: BestPracticeScannerList
    plural: bestpracticescanners
    shortNames:
    - bps
    singular: bestpracticescanner
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.scanInterval
      name: Interval
      type: string
    - jsonPath: .status.summary.compliant
      name: Compliant
      type: integer
    - jsonPath: .status.summary.nonCompliant
      name: NonCompliant
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BestPracticeScanner is the Schema for the bestpracticescanners
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BestPracticeScannerSpec defines the desired state of BestPracticeScanner.
            properties:
              checks:
                description: Checks is an optional list of check names to run. Empty
                  means run all.
                items:
                  type: string
                minItems: 1
                type: array
              labelSelector:
                description: LabelSelector filters pods to scan.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              scanInterval:
                description: ScanInterval defines the interval between periodic scans
                  (e.g. "5m"). Omit for one-shot.
                pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                type: string
              suspend:
                description: Suspend pauses scanning when set to true.
                type: boolean
              targetNamespace:
                description: TargetNamespace is the namespace to scan. Defaults to
                  the CR's namespace if omitted.
                type: string
            type: object
          status:
            description: BestPracticeScannerStatus defines the observed state of BestPracticeScanner.
            properties:
              lastScanTime:
                description: LastScanTime is the timestamp of the last completed scan.
                format: date-time
                type: string
              nextScanTime:
                description: NextScanTime is the scheduled time for the next scan.
                format: date-time
                type: string
              phase:
                description: Phase is the current phase of the scanner.
                type: string
              summary:
                description: Summary holds aggregate results from the last scan.
                properties:
                  compliant:
                    description: Number of compliant checks.
                    type: integer
                  error:
                    description: Number of checks that errored.
                    type: integer
                  nonCompliant:
                    description: Number of non-compliant checks.
                    type: integer
                  skipped:
                    description: Number of skipped checks.
                    type: integer
                  total:
                    description: Total number of checks run.
                    type: integer
                required:
                - compliant
                - error
                - nonCompliant
                - skipped
                - total
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
